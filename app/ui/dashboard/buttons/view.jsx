import React from 'react';
import styles from '@/app/ui/dashboard/buttons/buttons.module.css'

const View = () => {

    return (
        <div>
            <button className={`${styles.button} bg-teal-600`}>view</button>
        </div>
    );
};

export default View;